<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:targetApi="33" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Camera Permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />
    <uses-feature android:name="android.hardware.camera" required="true" /> <!-- Camera required -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trojan0Project"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".MapEntrants" />
        <activity android:name=".UserSignUpActivity" />
        <activity android:name=".OrganizerSignUpActivity" />
        <activity android:name=".ViewProfile" />
        <activity android:name=".ViewEvents" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".OrganizerPageActivity" />
        <activity android:name=".BrowseProfileAdmin" />
        <activity android:name=".BrowseImagesAdmin" />
        <activity android:name=".JoinWaitlist" />
        <activity android:name=".EntrantInvited" />
        <activity android:name=".DisplayEventDetails" />
        <activity android:name=".EventActivity" />
        <activity android:name=".FacilityActivity" />
        <activity android:name=".EventsListActivityOrganizer" />
        <activity android:name=".EventDetailsActivityOrganizer" />
        <activity android:name=".ViewFinalEntrantsEventActivity" />
        <activity android:name=".CancelEntrants" />
        <activity android:name=".ListEvents" />
        <activity android:name=".ManualEntryActivity" />

        <!-- QR Code Scanner Activity -->
        <activity
            android:name=".QrScannerActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize" />

        <!-- Event Details Activity -->
        <activity
            android:name=".EventDetailsActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Preloaded Fonts -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDOFX_MpuEUR4kVKMLGhxLA9fkYM-f7BTI" />
    </application>

</manifest>
